node_modules/
.DS_Store
# Your own projects, captures, runs and AI reviews stay local.
data/
demo/runs/
design-evidence/
# Proof of the acceptance runs (CI keeps them as an artifact).
recordings/
acceptance.log
