{
  "name": "dogfood",
  "version": "0.2.0",
  "description": "An AI QA system: a local, page-by-page QA workspace: every page, its checks, its evidence, and what is still unproven.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "demo": "DOGFOOD_DATA=demo node server.mjs",
    "check": "node --check server.mjs && node --check public/app.js && DOGFOOD_DATA=demo node scripts/check-projects.mjs",
    "test": "node --test scripts/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
